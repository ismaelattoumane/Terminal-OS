"use client";

import { FormEvent, startTransition, useEffect, useState } from "react";
import { CalendarDays, Plus, Trash2 } from "lucide-react";

type CalendarItem = { id: string; title: string; start: string; end: string; type: string };
type Schedule = { id: string; dayOfWeek: number; startTime: string; endTime: string; location: string | null; subject: { name: string } | null };
const days = ["Lundi", "Mardi", "Mercredi", "Jeudi", "Vendredi", "Samedi", "Dimanche"];

export function CalendarWorkspace() {
  const [events, setEvents] = useState<CalendarItem[]>([]); const [schedules, setSchedules] = useState<Schedule[]>([]); const [title, setTitle] = useState(""); const [date, setDate] = useState(""); const [startTime, setStartTime] = useState("18:00"); const [endTime, setEndTime] = useState("19:00"); const [dayOfWeek, setDayOfWeek] = useState(0); const [feedback, setFeedback] = useState("");
  async function load() { const [eventResponse, scheduleResponse] = await Promise.all([fetch("/api/calendar"), fetch("/api/schedule")]); if (eventResponse.ok) setEvents(await eventResponse.json()); if (scheduleResponse.ok) setSchedules(await scheduleResponse.json()); }
  useEffect(() => {
    let cancelled = false;
    Promise.all([fetch("/api/calendar"), fetch("/api/schedule")]).then(async ([eventResponse, scheduleResponse]) => {
      const eventData = eventResponse.ok ? await eventResponse.json() : [];
      const scheduleData = scheduleResponse.ok ? await scheduleResponse.json() : [];
      if (!cancelled) startTransition(() => { setEvents(eventData); setSchedules(scheduleData); });
    }).catch(() => { if (!cancelled) setFeedback("Connecte-toi pour gérer ton calendrier."); });
    return () => { cancelled = true; };
  }, []);
  async function createEvent(event: FormEvent) { event.preventDefault(); const response = await fetch("/api/calendar", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ title, start: `${date}T${startTime}:00`, end: `${date}T${endTime}:00`, type: "personal" }) }); if (response.ok) { setTitle(""); setFeedback("Événement ajouté."); await load(); } else setFeedback("Impossible d'ajouter l'événement."); }
  async function createSchedule(event: FormEvent) { event.preventDefault(); const response = await fetch("/api/schedule", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ dayOfWeek, startTime, endTime }) }); if (response.ok) { setFeedback("Créneau ajouté."); await load(); } else setFeedback("Impossible d'ajouter le créneau."); }
  return <div className="workspace-page"><div className="workspace-heading"><div><p className="eyebrow">PHASE 2 · CALENDRIER INTERNE</p><h1>Calendrier</h1><p className="muted">Planifie tes événements et protège tes créneaux de révision.</p></div>{feedback && <span className="workspace-message">{feedback}</span>}</div><div className="calendar-grid"><section className="data-form"><h2 className="form-title">Nouvel événement</h2><form onSubmit={createEvent}><label>Titre<input value={title} onChange={(event) => setTitle(event.target.value)} placeholder="Rendez-vous" required /></label><label>Date<input type="date" value={date} onChange={(event) => setDate(event.target.value)} required /></label><div className="form-row"><label>Début<input type="time" value={startTime} onChange={(event) => setStartTime(event.target.value)} /></label><label>Fin<input type="time" value={endTime} onChange={(event) => setEndTime(event.target.value)} /></label></div><button className="primary-button" type="submit"><Plus size={16} /> Ajouter</button></form></section><section className="data-form"><h2 className="form-title">Emploi du temps</h2><form onSubmit={createSchedule}><label>Jour<select value={dayOfWeek} onChange={(event) => setDayOfWeek(Number(event.target.value))}>{days.map((day, index) => <option key={day} value={index}>{day}</option>)}</select></label><div className="form-row"><label>Début<input type="time" value={startTime} onChange={(event) => setStartTime(event.target.value)} /></label><label>Fin<input type="time" value={endTime} onChange={(event) => setEndTime(event.target.value)} /></label></div><button className="secondary-button" type="submit"><CalendarDays size={16} /> Ajouter un cours</button></form></section></div><section className="calendar-board"><div className="list-heading"><h2>Cette semaine</h2><span>{events.length} événement(s)</span></div>{events.length ? events.map((event) => <div className="calendar-event" key={event.id}><span className="event-color" /><div><strong>{event.title}</strong><span>{new Date(event.start).toLocaleString("fr-FR", { dateStyle: "medium", timeStyle: "short" })} - {new Date(event.end).toLocaleTimeString("fr-FR", { timeStyle: "short" })}</span></div><b>{event.type}</b></div>) : <p className="empty-state">Aucun événement personnel pour le moment.</p>}</section><section className="calendar-board"><div className="list-heading"><h2>Créneaux de cours protégés</h2><span>{schedules.length}</span></div>{schedules.length ? schedules.map((schedule) => <div className="calendar-event" key={schedule.id}><span className="event-color school" /><div><strong>{days[schedule.dayOfWeek]}</strong><span>{schedule.startTime} - {schedule.endTime}{schedule.location ? ` · ${schedule.location}` : ""}</span></div><button className="delete-button" onClick={() => fetch(`/api/schedule/${schedule.id}`, { method: "DELETE" }).then(load)} aria-label="Supprimer le créneau"><Trash2 size={15} /></button></div>) : <p className="empty-state">Ajoute les horaires de tes cours pour que le planner les évite.</p>}</section></div>;
}